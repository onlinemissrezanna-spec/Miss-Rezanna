{
  "name": "miss-rezanna-root",
  "version": "1.0.0",
  "scripts": {
    "build": "cd backend && npm install && npx prisma generate",
    "start": "cd backend && npx prisma db push --accept-data-loss && node src/server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
